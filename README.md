# 预设条目对比、编辑与拖拽迁移

## 1.6.0 性能优化

- 差异状态、ID 映射和条目顺序改为缓存，只在导入、编辑或拖拽完成后重建一次。
- 列表渲染不再为每个条目重复创建 Map。
- 不再对每个列表条目反复 JSON.stringify 完整正文。
- 拖动经过目标位置时只更新插入线，不进行差异计算或列表重绘。
- 放置后只刷新受影响的列表，不重绘整个界面。
- 拖拽后仍保持内容对比区域收起。