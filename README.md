# 预设条目对比、编辑与拖拽迁移

## 1.7.0

- 身份可选择系统、用户、AI 助手，导出值分别为 system、user、assistant。
- 触发器默认导出为空数组，表示所有类型（默认）；也可明确写入酒馆六种触发类型。
- 位置可选择提示词管理器或聊天中，导出值分别为 0、1。
- 只有聊天中显示深度和顺序，保存为数字。
- 拖拽经过时不再查询整个文档。
- 拖拽放置后增量更新数据缓存和现有 DOM，不重新比较全部长正文或重建数百个条目。