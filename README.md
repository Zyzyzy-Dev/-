# 预设条目编辑器

旧版预设与新版预设在同一界面左右并排显示，每一侧都可以独立浏览、搜索、编辑、新建、删除和导出条目。

## 界面

- 左栏固定为旧版预设，右栏固定为新版预设。
- 两栏各自包含导入、导出、搜索、状态筛选和条目列表。
- 两边可同时打开不同条目并修改，不需要切换标签或窗口。
- 每栏上半部分是条目列表，下半部分是编辑区。
- 窄屏设备会自动改为上下排列。

编辑条目时会保留完整 JSON 中的未知字段；导出的文件不会覆盖原预设。
