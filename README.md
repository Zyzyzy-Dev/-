# 预设条目对比、编辑与拖拽迁移

## 1.8.0

- 位置选项更新为相对（0）和聊天中（1）。
- 触发器可选择：所有选项（默认）、正常、续写、AI 帮答、备选回复、重新生成、静默。
- 默认触发器导出为 []；其他选项导出为对应的单元素 injection_trigger 数组。
- 展开详情后，旧版与新版各显示一个可直接编辑的条目表单。
- 覆盖/迁移按钮横向排列；收起为右上角 × 图标。
- 两份预设的姓名、身份、触发器、位置、深度、顺序与正文可独立保存并导出。