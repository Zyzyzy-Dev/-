# 预设条目对比、编辑与拖拽迁移

## 2.3.2

- 新版内部拖拽到另一个分组时，会根据落点所在分组同步更新 groupId。
- 拖到未分组区域会清除 groupId。
- 条目顺序仍按照 prompt_order 保存。
- 移动端取消条目列表固定高度，插件页面可以连续向下滚动查看更多条目。
- 保留长按拖拽，同时普通手指滑动不会被列表内部滚动截断。
- 项目选择框增加边框和最小宽度，提升移动端可见性。